function myFunction(arg1, arg2){

}

const myOtherFunction = function(arg1, arg2){

}


const add = (x, y) => x + y;

const double = number => number * 2;

const getPersonData = () => ({
    name: 'Manuel',
    age:30,
    job:'programmer',
});


const myArroeFunction = (arg1, arg2) => arg1 + arg;
